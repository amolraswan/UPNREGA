# UPNREGA

Shiny app and PDF downloader for Uttar Pradesh NREGA daily person-days data.

The app lets the user choose a UP district, enter a recent date, scrape NMMS
attendance data, view summary tables, and download Excel outputs. The standalone
`download_pdfs.R` script saves muster roll pages as PDFs for the district and
date configured at the top of the file.
